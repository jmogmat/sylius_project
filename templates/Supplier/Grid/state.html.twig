{% set map = {
    'new': {'color': 'blue','icon': 'bell'},
    'trusted': {'color': 'yellow','icon': 'star'}
}  %}

<span class="ui label blue {{ map[data].color }}">
    <i class="icon {{ map[data].icon }}"></i> {{ data|capitalize }}
</span>
